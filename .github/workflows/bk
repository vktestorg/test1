jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo " The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."

jobs:
  check_team_membership:
    runs-on: ubuntu-latest

    steps:
      - name: Check User Team Membership
        run: |
          PAT=${{ secrets.VK_PAT1 }}
          MEMBERSHIP=`curl -s -H "Authorization: Bearer $PAT"  https://api.github.com/orgs/vktestorg/teams/test1/memberships/vikramk20`
          echo $MEMBERSHIP
          if [[ $MEMBERSHIP == *"\"state\": \"active\""* ]]; then
              echo "User $USERNAME is a member of team $TEAM in organization $ORG."
          else
              echo "User $USERNAME is not a member of team $TEAM in organization $ORG."
          fi


